{
  "name": "mern-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    
    "client": "npm start --prefix frontend",
    "server": "npm run dev --prefix server",
    "dev": "concurrently \"cd npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
